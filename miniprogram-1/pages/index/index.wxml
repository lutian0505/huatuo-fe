<!--index.wxml-->
<view class="container">
  <view class="page-section" wx:if="{{!loginSuccess}}">
    <view class="weui-cells weui-cells_after-title">
      <view class="weui-cell weui-cell_input">
        <input class="weui-input" auto-focus placeholder="请填写你的staffid" bindinput="getStaffid"/>
      </view>
    </view>
  </view>
  
  <view class="userinfo">
    <button wx:if="{{!loginSuccess}}" open-type="getUserInfo" bindtap="binding"> 绑定信息 </button>
    <block wx:else>
      <text>News Waterflow. To Be Continue...</text>
    </block>
  </view>
</view>
